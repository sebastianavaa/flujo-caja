Traffic-light chip showing budget health — green OK, yellow warning, red over-limit. Used in the billing hero.

```jsx
<StatusChip status="ok" />
<StatusChip status="warn" />
<StatusChip status="over" label="✕ EXCEDE" />
```

`status`: `ok | warn | over`. Defaults to Spanish labels; pass `label` to override.
